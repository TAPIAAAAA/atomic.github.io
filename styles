/* Estilo para el cuerpo con la imagen de fondo */
body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-image: url('atomic.jpg'); /* Imagen de fondo */
    background-size: cover; /* Que cubra toda la pantalla */
    background-position: center; /* Centrada en la pantalla */
    background-repeat: no-repeat;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    color: white;
}

/* Estilo del título */
.title {
    font-size: 48px;
    text-align: center;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
    margin-bottom: 20px;
}

/* Contenedor del video con bordes */
.video-container {
    width: 620px; /* Un poco más ancho que el video */
    padding: 10px;
    background-color: rgba(255, 255, 255, 0.8); /* Fondo blanco semi-transparente */
    border: 4px solid #FF6347; /* Borde color rojo tomate */
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra */
}

video {
    width: 100%;
    height: auto;
    border-radius: 5px;
}
